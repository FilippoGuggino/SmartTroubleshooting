{
  "type": "object",
  "properties": {
    "trainingPercent": {
      "description": "Percentage of the records to be used to build the training set",
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "testPercent": {
      "description": "Percentage of the records to be used to build the test set",
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "validationPercent": {
      "description": "Percentage of the records to be used to build the validation set",
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "segregationPeriod": {
      "description": "Period (in seconds) of execution of segregation procedure",
      "type": "number",
      "minimum": 5
    },
    "output_json_directory": {
      "description": "Directory path for json output",
      "type": "string"
    },
    "output_csv_directory":  {
      "description": "Directory path for json output",
      "type": "string"
    },
    "input_directory":  {
      "description": "Directory path for ingestion input",
      "type": "string"
    },
    "json_schemas_directory":  {
      "description": "Directory path for json schemas",
      "type": "string"
    }
  },
  "required": [
    "trainingPercent",
    "testPercent",
    "validationPercent",
    "segregationPeriod",
    "output_json_directory",
    "output_csv_directory",
    "input_directory",
    "json_schemas_directory"
  ]
}